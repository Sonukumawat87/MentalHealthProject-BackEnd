# Server (IMPORTANT for cloud)
server.port=${PORT:8080}

# ❌ REMOVE local MySQL (will crash on deploy)
# spring.datasource.url=jdbc:mysql://localhost:3306/mentalsupport?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
# spring.datasource.username=root
# spring.datasource.password=root

# Temporary (no DB)
spring.jpa.hibernate.ddl-auto=none

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT
app.jwt.secret=my-super-secret-key-for-mental-support-project-2026
app.jwt.expiration-ms=604800000

# CORS (allow frontend)
app.cors.allowed-origins=*
